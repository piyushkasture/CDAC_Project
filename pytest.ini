[pytest]

addopts =
    --browser=chromium
    --browser=firefox
    --headed
    --screenshot=on
    --tracing=on
    --html=reports/assignment.html --capture=tee-sys
    --alluredir=reports/allure-results
    -s
    -v
    # Parallel Execution
    -n 1